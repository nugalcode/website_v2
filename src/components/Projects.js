import { forwardRef } from 'react';
import '../css/Projects.css';

const Projects = forwardRef((props, ref) => {
    return (
        <div id="Projects" ref={ref}> PROJECTS SECTION </div>
    )
})

export default Projects;